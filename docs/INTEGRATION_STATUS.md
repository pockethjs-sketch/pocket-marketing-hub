# 운영 데이터 연동 현황

## 현재 결론

### 2026-09-03 업무 하위 진행사항 화면

- 좌측 `업무 → 진행사항` (`#tasks/progress`)은 기존 업무 권한과 `projectId:tasks` 캐시를 재사용한다. 별도 업무 원장이나 시안 데이터를 배포하지 않는다. 고객 권한 관리의 업무 설명에 진행사항을 포함하며, 진행사항만의 독립 권한은 추가하지 않았다.
- 상단은 실제 상태 기준 `진행된 업무`와 한국시간 월요일~일요일의 `이번 주 진행 예정 업무`다. 명시적으로 비운 간트와 비연속 일정은 그대로 존중하고, 완료 업무는 예정 목록에서 제외한다. 진행 중인 업무는 실제 일정에 따라 두 영역에 함께 나타날 수 있다.
- 지난 회의는 별도 비차단 조회로 불러오며, 회의록 권한이 없는 고객에게는 조회하지 않는다. 서버의 공개범위 검사에 더해 고객에게 `CLIENT` 회의만 표시한다. 미래 회의는 지난 회의에 포함하지 않는다.
- 확인 요청은 기존 `project_issues`를 재사용한다. 제목은 `related_task_text`, 본문은 `body_text`, 확인 담당은 `owner_text`, 링크는 `completion_url`, 답변·추가 메모는 `remarks`다. 기존 이슈표와 같은 행을 읽고, `mutate_project_issue`의 행 버전·감사 로그·공통 저장 잠금을 유지한다.
- 등록·답변·확인 완료는 현재 서버 권한에 맞춰 포켓·NS 편집 계정만 가능하다. 고객 계정은 조회 전용이며 상호 고객 답변 권한은 이번 배포에서 확대하지 않았다. 실제 파일 업로드가 아닌 자료 URL 등록이다.
- 단위·계약 검사 172개와 분리된 테스트 서비스 브라우저 검사(등록·답변·완료·재요청, 실패 초안 보존, 고객 비공개, 프로젝트 전환, 모바일 폭)를 통과했다. 테스트 데이터를 운영 원장에 넣지 않았다.
- 운영 Supabase의 업무·회의 조회 및 이슈 저장 RPC가 존재하고 로그인 사용자에게 실행 권한이 있으며, 익명 실행은 차단된 것을 읽기 전용 조회로 확인했다. 이번 변경은 기존 RPC와 RLS를 재사용하며 DB 구조는 변경하지 않는다.

### 2026-09-03 독립 일정표 메뉴 제거·대시보드 2종 기획

- 작동하지 않는 별도 `일정표` 항목은 좌측 운영 메뉴에서 제거했다. `#tasks/schedule` 라우트와 업무 페이지 내부의 `일정표 / 간트 / 업무 로그` 전환, 고객의 업무 페이지 권한은 유지해 기존 링크와 저장 기능에는 영향이 없다.
- 신규 화면은 `고객 브리핑`과 `운영 관제`로 목적을 분리한다. 전자는 프로젝트 단위 읽기 전용 요약, 후자는 전 프로젝트 지연·막힘·마감·KPI 위험과 담당자 개입 순서를 보여주는 내부 화면이다.
- 상세 화면 구조, 데이터 판정 규칙, 접근 권한과 단계별 구현안은 `docs/DASHBOARD_PLAN.md`를 기준 문서로 사용한다. 새 화면 구현 전까지 기존 총괄 현황과 데이터 원장은 변경하지 않는다.

### 2026-09-03 업무 일정표 완료·열 배치 조정

- 업무 일정표에서 상태를 `완료`로 바꾸면 같은 업무 mutation에 `status_code = DONE`과 `progress_percent = 100`을 함께 저장한다. 상태 저장 성공과 진행률 저장 성공이 갈라지지 않는다.
- 일정은 브라우저의 실제 날짜 입력값 `YYYY-MM-DD`를 유지하되 셀에는 `월.일`만 표시한다. 날짜 선택기를 열면 연도를 포함한 전체 날짜를 확인·수정할 수 있다.
- 전체 표 폭 1244px은 유지하면서 업무명 열을 170px에서 290px로 늘리고, 세부내용은 250px에서 200px로 20% 줄여 최대 2줄까지만 바로 표시한다. 일정 열은 196px에서 126px로 줄였다.

### 2026-09-03 간트 원본 비율 고정

- 기준 `캠페인 스케줄 관리.html`의 간트 치수인 업무명 열 280px, 날짜 열 28px, 업무 행 32px, 날짜 머리글 21px + 42px를 그대로 사용한다.
- 간트 전체 폭, 업무명 열, 날짜 열에 고정 폭을 함께 지정해 상위 컨테이너의 남는 폭 때문에 열이 늘어날 여지를 제거했다. 회귀 검사는 CSS 선언값뿐 아니라 실제 렌더링 경계 폭도 280px/28px인지 확인한다.
- 배포 화면 캡처는 GitHub Pages 도메인에 Chrome 125% 확대가 적용되어 224px 좌측 메뉴가 약 280px로, 260px 업무 열이 약 325px로 표시되고 있었다. 브라우저 확대는 사이트별 설정이므로 배포 페이지에서 `Ctrl+0`으로 100%를 복원해야 기준 HTML과 1:1 비교된다.

### 2026-09-03 업무 일정표 열 밀도·가독성 조정

- 매체명은 그룹 첫 행에만 표시하지 않고 모든 업무 행에 반복 표시한다. 같은 매체 그룹의 경계선과 색상 레일은 유지한다.
- 시작일·종료일을 2개 열에서 하나의 `일정` 셀로 합쳤다. 두 날짜 사이를 이동하는 동안은 로컬 초안만 바꾸고 일정 셀을 벗어날 때 두 날짜와 간트 배열을 한 번의 mutation으로 저장한다.
- 세부내용 입력은 고정 1줄 높이와 수동 펼치기를 제거하고 내용 길이에 맞춰 자동 줄바꿈·행 높이 확장으로 표시한다. 기간·상태·담당 열과 버튼의 좌우 여백을 줄이고 전체 표 폭을 1350px에서 1244px로 축소했다. 업무 원장 필드와 저장 대상은 변경하지 않았다.

### 2026-09-03 견적 검토 비율·중복 파생 업무 수정

- 견적 검토창은 기준 `캠페인 스케줄 관리.html`과 같은 데스크톱 960px 폭, 조밀한 헤더·메타데이터·기간·합계·항목표·하단 조작 순서로 맞췄다. 일반 생성 모달의 620px 폭 규칙이 뒤에서 견적창 폭을 덮어쓰던 CSS 우선순위 충돌을 전용 선택자로 차단했다.
- `메디신피아_9월 캠페인 견적서.pdf`의 11개 항목은 수량 합계가 27개다. 기존 29개 표시는 유튜브 채널 세팅의 `채널 아트·프로필 제작`과 네이버 블로그 세팅의 `대문 배너·프로필 제작`을 읽고도 디자인 파생 업무를 각각 한 번 더 만든 결과였다.
- 파생 디자인 옵션은 유지하되 견적 세부내용에 해당 디자인·썸네일 산출물이 명시된 항목은 추가 생성하지 않는다. 같은 견적 회귀 검사는 `11개 항목 / 수량 27 / 생성 27 / 중복 파생 0`을 고정 검증한다. 이번 수정은 검토 미리보기와 이후 신규 생성에 적용되며 이미 저장된 업무는 자동 삭제하지 않는다.

### 2026-09-03 매체 그룹 정렬·일정 화면 축소

- 일정표와 간트는 `06_업무.sort_order`로 받은 원장 순서를 보존한 채 같은 매체의 업무를 연속으로 묶은 공통 배열을 사용한다. 날짜순으로 일정표만 재정렬해 매체가 흩어지고 두 화면의 행 순서가 달라지던 표시 경로를 제거했다.
- 일정표는 각 매체 그룹의 첫 행에만 매체명을 표시하고 이후 행은 빈 매체 셀과 동일 색상 레일을 유지한다. 그룹 시작에는 구분선을 추가해 YouTube·Instagram·네이버블로그·Ads 등의 경계를 바로 식별할 수 있다.
- 일정표 폭은 1350px 고정 레이아웃으로 바꿔 넓은 화면에서 열이 임의 확장되지 않게 했고, 본문 행은 32px 기준으로 축소했다. 당시 간트를 260px/26px/30px로 축소했으나 이후 기준 HTML과의 1:1 비율 요구에 따라 280px/28px/32px로 복구했다. 저장 필드, 원장 순서와 Supabase mutation 계약은 변경하지 않았다.

### 2026-09-03 견적서 기반 프로젝트 생성

- 기준 HTML의 `견적서 불러오기` 흐름을 운영 화면 상단에 연결했다. PDF·엑셀·CSV·TSV를 읽은 뒤 고객사/프로젝트/기간과 항목별 포함 여부·열 매핑·수량 분할·파생 디자인 업무를 검토할 수 있다.
- 검토 결과는 현재 프로젝트에 업무로 추가하거나 새 고객사·프로젝트와 업무를 함께 생성할 수 있다. 견적 원본 진행률을 임의 계산하지 않고 신규 업무는 0%·미착수로 저장한다.
- 새 프로젝트와 업무는 `create_project_from_quote`, 기존 프로젝트 추가는 `import_quote_tasks`로 Supabase에 한 번에 확정한다. 요청당 최대 600개, 중복 요청 재실행 안전, 일부 실패 전체 롤백을 적용했다. 익명·고객 계정 실행은 차단하고 포켓·NS 내부 권한만 허용한다.
- 견적 메타데이터와 합계는 `projects.quote_data`에 보존하되 고객 응답에서는 마스킹한다. 원격 DB의 컬럼·RPC·authenticated 전용 실행 권한을 확인했고 전체 테스트 161개와 로컬 PostgreSQL 계약 검사에서 권한 격리·원자 생성·롤백을 통과했다.

### 2026-09-03 업무 일정표 밀도 조정

- 기준 HTML의 38px 안팎 행 밀도에 맞춰 일정표 헤더·행·입력 컨트롤·관리 버튼을 축소했다.
- 12개 열 전체 최소 폭을 1544px에서 1350px로 줄이고 고정 매체/업무 열도 함께 축소했다.
- 저장 계약과 Supabase 데이터 구조는 변경하지 않았다.

### 2026-09-03 NS 프로젝트 생성·업무 보관 삭제

- 포켓·NS 내부 계정은 전역 상단의 `프로젝트 추가`에서 프로젝트 회사명, 프로젝트명, 기간, 설명을 입력해 새 운영 단위를 생성한다. 현재 화면 계약은 회사 1개와 대표 프로젝트 1개가 한 쌍이므로 두 행을 함께 만들고, 생성자에게 `ADMIN`(포켓) 또는 `EDIT`(NS) 멤버십과 운영 페이지 권한을 같은 트랜잭션으로 부여한다.
- 고객 계정과 비활성 계정은 `create_project` RPC에서 거부한다. 같은 저장 ID 재호출은 기존 응답을 반환하고, 같은 회사명으로 동시에 생성하는 요청은 DB 잠금과 중복 검사로 차단한다. 브라우저에는 고객사·프로젝트·멤버십 직접 INSERT 권한을 주지 않는다.
- 일정표와 간트의 각 업무 행에 `수정 / 삭제` 조작을 명시했다. 삭제는 확인 버튼을 한 번 더 눌러야 실행되며 `mutate_task(ARCHIVE)`로 `archived_at`, 변경자, mutation, 감사 로그를 남긴 뒤 활성 화면과 캐시에서만 제거한다. 업무 행은 물리 삭제하지 않는다.

### 2026-09-03 상단 회사 선택·포켓컴퍼니 프로젝트

- 회사 단위 프로젝트 선택을 좌측 고객사 레일에서 전역 상단 바로 옮겼다. 상단에는 접근 가능한 회사명이 가로 탭으로 표시되고, 좌측에는 현재 프로젝트의 화면 메뉴만 남는다.
- Supabase에 `포켓컴퍼니` 고객사와 `포켓컴퍼니 내부 운영` 프로젝트를 독립 원장으로 추가했다. 고객 공개는 꺼 두고 NS·고객 멤버십은 생성하지 않아 포켓 관리자에게만 기본 노출한다.
- 회사 전환은 bootstrap의 실제 `client_id / project_id`를 사용하고 일정표로 이동한다. 조회 캐시와 저장 요청은 계속 프로젝트 ID로 분리되므로 UND·무극·포켓컴퍼니 데이터가 서로 섞이지 않는다.

### 2026-09-03 일정표 직접 편집 전환

- 기준 파일 `캠페인 스케줄 관리.html`의 표 구조를 일정표 탭에 적용했다. 열은 `매체 / 업무 / 세부내용 / 시작일 / 종료일 / 기간 / 진행률 / 상태 / 담당 / 완료링크 / 비고` 11개이며, 간트와 동일한 Supabase 업무 행을 사용한다.
- 업무명·세부내용·날짜·진행률·완료링크·비고는 셀에서 바로 수정하고 포커스가 빠질 때 한 필드만 저장한다. 상태는 `미착수 → 진행 → 완료 → 보류`, 담당은 `포켓 → NS → 현재 고객사` 순으로 클릭 변경한다. 내부 쓰기 계정만 조작할 수 있고 고객 계정은 같은 표를 읽기 전용으로 본다.
- 날짜 편집은 시작일·종료일만 바꾸지 않고 두 날짜 사이의 `schedule_dates_json`도 같은 mutation에 포함한다. 따라서 DB 일정 정규화 트리거가 이전 간트 배열로 새 날짜를 되돌리거나, 일정표와 간트가 서로 다른 날짜를 표시하는 경로를 차단했다.
- 모든 수정은 기존 Supabase `mutate_task` RPC, `row_version`, `mutation_id`, 감사 로그를 그대로 사용한다. 프런트·Apps Script 계약 테스트 151개와 프로덕션/Sites 빌드를 통과했다.

### 2026-09-03 운영 접속 장애 수정

- Supabase 전환 분기에서 빌드 환경의 정상 Apps Script URL을 빈 문자열로 덮어쓰던 초기화 회귀를 확인했다. 이 때문에 하이브리드 어댑터 생성이 로그인 화면 렌더링 전에 `missing_api_url`로 중단됐다.
- Supabase를 원장으로 쓰더라도 아직 이관하지 않은 Sheets 화면과 백그라운드 호환 세션에는 Apps Script URL이 필요하므로, 검증된 런타임 URL을 그대로 하이브리드 어댑터에 전달하도록 수정했다.
- 이번 장애는 읽기·쓰기 요청 이전의 프런트 초기화 실패이므로 Supabase 및 Google Sheets의 저장 데이터에는 변경이나 유실이 없다. 동일 회귀를 막는 데이터 소스 단위 테스트를 추가했다.

### 2026-09-03 실행계획 Supabase 전환

- `클라이언트 공유용`과 `내부 실행계획` 조회를 Apps Script의 `project_plan`에서 Supabase `plans`·`plan_sections` RLS 조회로 이전했다. 실행계획 진입은 더 이상 백그라운드 Sheets 세션을 기다리거나 Google Sheets를 읽지 않는다.
- 기존 Google Sheets의 `17_실행계획`·`18_실행계획섹션`은 삭제하지 않고 검증·복구 기준본으로 유지한다. Supabase에는 최신 승인 HTML의 클라이언트 공유용 10개 섹션과 내부 실행계획 21개 섹션을 이관했다.
- 계획 이관은 Pocket 관리자만 호출할 수 있는 `import_project_plan` 트랜잭션으로 제한한다. 고객 공유본은 `CLIENT`, 내부 본문은 `PROJECT_TEAM`, 내부 부록은 `POCKET_ONLY`로 고정하며 RLS가 프로젝트·페이지 권한과 조직별 공개 범위를 다시 검사한다.
- 실계정 조회 결과 Pocket은 31개 섹션 전체, NS는 `POCKET_ONLY` 11개를 제외한 20개 섹션만 조회했다. 계획 테이블과 섹션 테이블 모두 RLS가 활성화되어 있고 익명 조회는 허용하지 않는다.

### 2026-09-03 전면 저장 전환 작업

- 로그인은 Apps Script 인증 완료를 기다리지 않고 Supabase Auth의 아이디 매핑(`아이디@hub.local`)으로 직접 처리한다. 기존 Sheets 전용 화면에 필요한 레거시 세션만 로그인 뒤 백그라운드에서 준비하며 첫 화면을 막지 않는다.
- 신규 고객 계정·허용 페이지는 Supabase를 기준 원장으로 저장하되, 콘텐츠·성과 추적·파일·세부 로그처럼 아직 Sheets에 남은 화면의 로그인 호환성을 위해 Apps Script 계정에도 복제한다. 복제 실패는 저장 성공으로 숨기지 않는다.
- 업무에 이어 데일리 회의록, KPI 정의·실적 조회, 고객 계정·프로젝트 접근 권한을 Supabase 단일 원장으로 이전한다. 쓰기는 페이지 권한, 낙관적 `row_version`, 중복 방지 `mutation_id`, 감사 로그를 DB 트랜잭션에서 함께 확정한다.
- Google Sheets의 기존 21개 탭은 삭제하거나 덮어쓰지 않는다. Supabase 업무 스냅샷은 별도 숨김·보호 탭에 매일 03:00 KST 미러링한 뒤 기존 전체 스프레드시트 복제 백업에 포함한다. 보호 편집자 제한이 계정 정책상 실패해도 경고형이 아닌 시트 보호가 적용됐는지를 별도로 검증한다.
- 전환 완료 기준은 원격 로그인·조회·생성·수정·보관 E2E, 고객/실행사 권한 격리, 자동 백업 실호출, 전체 테스트·프로덕션 빌드·보안 Advisor 재검사다. 검증 전에는 기존 Sheets 데이터를 롤백 기준본으로 유지한다.
- 최종 실백업은 2026-09-03 09:07 KST에 완료했다. Supabase 업무 101건을 `[백업] Supabase업무`에 기록해 `hidden=true`, `protected=true`를 확인했고, 이를 포함한 전체 Drive 복제본의 manifest 검증도 통과했다. GitHub Actions 실행은 `33698090402`다.

2026-09-03 현재는 **Supabase Auth + 업무·업무 로그·회의록·KPI·권한 원장과 실행계획 조회를 전환하고, 콘텐츠·성과 추적·파일·세부 로그는 기존 Apps Script/Google Sheets를 유지하는 단계적 운영 구조**입니다. Google Sheets는 삭제·초기화하지 않았으며 원본 101건과 기존 계획 기준본을 그대로 보존합니다.

- 원격 Supabase 프로젝트에 PostgreSQL 마이그레이션 12개와 공개 테이블 23개를 적용했습니다. 모든 공개 테이블은 RLS를 사용하고 `anon`의 업무 접근과 브라우저의 원본 테이블 직접 쓰기를 차단합니다.
- Sheets의 UND 78건·무극 23건, 합계 101건을 Supabase로 복제했습니다. 프로젝트별 건수, `legacy_id`, 업무 제목·상태·분야·담당·일정 배열의 누락과 중복은 0건입니다.
- 포켓·NS 로그인은 Supabase Auth가 직접 처리합니다. 기존 Sheets 전용 화면용 Apps Script 세션은 로그인 뒤 백그라운드에서만 준비합니다. 업무·회의록·KPI 조회/저장과 업무 로그는 Supabase RPC만 사용합니다.
- 총괄은 콘텐츠·승인·KPI 등 아직 이관하지 않은 값은 Sheets에서 읽고, 업무 건수·단계·분야 집계만 Supabase 업무 원장으로 교체합니다. 업무 저장 뒤 총괄 건수가 시트 때문에 뒤처지지 않도록 한 조치입니다.
- 실행계획은 Supabase에서 직접 조회합니다. 콘텐츠·성과 추적·파일·세부 로그는 아직 Apps Script/Sheets를 사용하므로 이 영역까지 전환되기 전에는 Sheets를 폐기하거나 읽기 전용으로 고정하면 안 됩니다.
- 업무는 현재 Supabase 단일 쓰기입니다. Sheets 업무 행에 자동 이중 쓰기하지 않으므로 장기적으로 두 원장을 동시에 편집해서는 안 됩니다. 장애 시에는 쓰기를 먼저 잠근 뒤 마지막 동기화 지점을 대조하고 기능 플래그를 `sheets`로 되돌립니다.
- 직접 로그인 실측은 Pocket 0.585초, NS 0.292초였고 bootstrap까지 각각 0.762초, 0.465초였습니다. Sheets 전용 화면은 백그라운드 호환 세션을 기다릴 수 있어 별도 병목으로 남습니다.
- 프런트·Apps Script 테스트 151개, Supabase 계약 검사, 프로덕션 빌드, 운영 의존성 감사 0건을 통과했습니다. 회의록·KPI·고객 권한과 실행계획 역할 격리도 통과했습니다.

- 기준 파일 `캠페인 스케줄 관리.html`의 seed JSON을 현재 `06_업무`에 이관했습니다. 무극 23건은 완료 5 / 진행 15 / 미착수 3, 담당 포켓 4 / NS 19이며, UND 78건은 미착수 78, 담당 포켓 4 / NS 74입니다. 두 프로젝트 모두 원본 제목·세부내용·시작일·종료일·일별 간트 배열·상태·담당·완료링크·비고가 일치하고 `source_task_id` 누락·중복은 0건입니다.
- 이관은 관리자 전용 `migrate_campaign_schedule_v1` 유지보수 작업으로 수행합니다. 대상 프로젝트 원장이 비어 있지 않으면 기존 이관 ID 집합과 정확히 일치하는 재검증 요청 외에는 거부하고, 첫 쓰기 또는 보정 전 `mhRunDailyBackup(true)`의 21개 시트 해시 검증이 성공해야 진행합니다. 이관·보정은 일반 사용자 업무 로그가 아니라 `TASK_BATCH / MIGRATE|RECONCILE` 감사 이벤트 한 건으로 남깁니다.
- 최종 이관 보정 직전 백업 `PocketMarketingHub_20260902_195807`은 21/21 시트 일치, 누락 0, 불일치 0입니다. 최초 이관에서 물리 `schedule_dates_json` 열이 아직 없어 경계일만 기록된 상태를 사후 비교기가 탐지했고, 열 생성 후 101건의 일별 배열을 원본과 다시 맞췄습니다. 날짜·숫자·간트 JSON 비교도 정규화해 동일 원본 재실행은 `skipped: true`, 추가 백업 없음으로 검증했습니다. 현재 Web App은 @101, 백엔드는 `2026-09-02-campaign-schedule-migration-v41`입니다.
- 2026-09-02 신규 업무 재입력을 위해 `06_업무`에서 UND 146행(활성 144 + 기존 보관 2), 무극 20행을 물리 삭제했습니다. 다른 프로젝트 행과 고객사·프로젝트·실행계획·콘텐츠·성과·회의록·활동로그는 삭제하지 않았습니다. `07_업무의존성`에는 두 프로젝트 대상 행이 0건이었습니다.
- 삭제 전 전체 원장을 `PocketMarketingHub_20260902_183703`으로 복제하고 시트별 해시 검증에서 누락·불일치 0건을 확인했습니다. 삭제 직후 운영 `tasks` API는 UND 0건, 무극 0건을 반환했으며, 이후 위 기준 HTML 101건을 새로 이관했습니다. 실행자와 프로젝트별 삭제 건수는 `15_활동로그`의 `TASK_BATCH / DELETED / COMMIT`으로 보존했습니다.
- 삭제에 사용한 일회성 관리 경로는 실행 직후 운영 코드에서 제거하고 같은 Web App을 @98로 재배포했습니다. 승인 계획을 다시 채우는 `sync_und_task_structure`와 무극 프로비저닝은 과거 이관용 코드이므로 신규 업무 수동 입력 중에는 실행하지 않습니다.
- 운영 원장은 백업 후 사용자·프로젝트 권한·공개 범위를 정리했습니다.
- UND 팀 트래커 승인 업무 144건은 과거 `APPROVED_PLAN` 원천으로 복구했던 이관 기록입니다. 단계별 P0 57건 / M1 31건 / M2 28건 / M3 28건, 분야별 마케팅 79건 / 디자인 35건 / 영상 30건의 구조는 `91_업무템플릿`과 문서에만 남아 있습니다. 현재 `06_업무`의 UND 78건은 이와 별개인 캠페인 HTML 9월 일정입니다.
- 2026-08-25 미팅을 반영한 최신 `UND_90일_실행계획_클라이언트공유용.html`을 `17_실행계획` 1행과 `18_실행계획섹션` 10행으로 구조화했습니다. 화면은 iframe이 아니라 현재 Pocket UI로 렌더링하며 원본 SVG·스타일·스크립트는 포함하지 않습니다.
- 내부 실행계획을 같은 17/18 원장에 설치하고 `CLIENT_SHARE` / `INTERNAL` 선택 API를 연결했습니다. 계획 2행·섹션 31행을 검증했으며 임시 마이그레이션 라우트와 내부 생성 번들은 최종 배포에서 제거했습니다. 본문은 `PROJECT_TEAM`, 실행팀 부록은 `POCKET_ONLY`이고 고객의 내부 계획 요청은 서버에서 거부합니다.
- Apps Script는 로그인, 서명 세션, 역할·프로젝트 권한, 읽기, create/update/archive, 중복 요청 방지, row version, PREPARE/COMMIT 활동로그를 구현했습니다.
- 로그인 UI는 ON 상태이며 공개 미리보기와 익명 업무 쓰기는 모두 OFF입니다. Pocket 계정은 `POCKET_MANAGER + ADMIN`, NS 계정은 `EXECUTOR_EDITOR + EDIT`로 분리했습니다. NS 계정의 권한 관리 API 접근은 서버에서 403으로 거부됩니다. 짧은 로그인 아이디는 서버에서 내부 계정 주소로 정규화하며, 비밀번호 원문은 프런트·원장·저장소에 저장하지 않고 Script Properties의 HMAC digest로만 검증합니다.
- 로그인 요청은 서명 세션과 허용 고객사·프로젝트·채널의 최소 `bootstrap`을 한 Apps Script 실행에서 함께 반환합니다. 기존처럼 로그인 후 `bootstrap`을 순차 호출하지 않으며, 구버전 서버의 평면 로그인 응답은 프런트 호환 경로에서만 별도 `bootstrap`을 호출합니다. 비로그인 `preview_bootstrap` 경로는 호환 코드만 남아 있으며 현재 설정에서는 `forbidden`으로 거부됩니다.
- 고객사는 조회 전용이며 내부 필드와 원시 활동로그를 받지 않습니다.
- 업무 원장은 승인 계획의 `plan_week`, `plan_note`를 저장합니다. API의 `contract_linked`는 `plan_note` 존재 여부로 파생하며 고객에게는 Boolean만 제공하고 계약·검수 사유 원문은 숨깁니다.
- 착수일은 Pocket 내부 사용자만 업무 화면에서 변경할 수 있고 `02_프로젝트.start_date`에 행 버전 충돌 보호와 함께 저장됩니다. 고객과 실행사는 값만 조회합니다.
- 업무 화면의 발행 실적은 `08_콘텐츠`에서 `PUBLISHED` 상태인 롱폼·숏폼·인스타그램·네이버 블로그를 연결 업무의 단계별로 자동 집계합니다. 수동 발행 카운터는 사용하지 않습니다.
- 데스크톱 탐색은 왼쪽에 `고객사 목록 / 현재 프로젝트 메뉴`를 항상 나열합니다. 좌우 화살표나 단계 순환으로 메뉴를 교체하지 않으며, 작은 화면에서만 일반 메뉴 아이콘으로 통합 서랍을 여닫습니다.
- 운영 빌드는 라이브 실패 시 데모 데이터로 대체하지 않습니다.
- 읽기는 실행 내 메모리 캐시와 180초 서버 캐시를 사용합니다. 85KB를 넘겨 캐시에서 빠지던 `06_업무` 같은 원장은 gzip으로 압축한 뒤 캐시하며, 캐시 키에는 스키마·백엔드 버전이 포함돼 재배포 전 권한값이 남지 않습니다. 저장은 테이블 캐시를 우회해 최신 행과 `row_version`을 확인하고 대상 원장 캐시를 폐기합니다. 완성 화면 응답 캐시는 사용자·역할·프로젝트·필터·프로젝트 epoch로 분리하며 저장·복구·권한 변경 성공 즉시 epoch를 증가시켜 이전 응답을 재사용하지 않습니다.
- 프런트는 현재 화면의 경량 전용 API만 호출합니다. 전체 탭을 한꺼번에 읽던 `project_snapshot` 선조회는 제거했고 `프로젝트 + 화면`별 최근 응답은 10분 보관합니다. 같은 탭의 새로고침은 사용자 ID가 일치하는 최근 bootstrap을 `sessionStorage`에서 즉시 표시한 뒤 서버 응답으로 교체합니다. 로그아웃 시 브라우저 캐시는 삭제됩니다.
- Apps Script의 단기 302 리다이렉트가 재사용돼 간헐적 404·무한 대기가 발생하지 않도록 모든 프런트 요청에 고유 `_mh` 값을 부여합니다.
- 인증된 포켓·NS·고객 읽기 응답은 같은 사용자·프로젝트·필터의 완성 응답을 120초, 실행계획·프로젝트 스냅샷을 300초 재사용합니다. 현재는 로그인 필수 운영이므로 익명 `CLIENT_VIEWER` 편집 경로는 사용하지 않습니다.
- API 관리 탭은 직접 수정으로 로그·버전을 우회하지 않도록 보호했습니다.
- Apps Script 권한 승인, Web App 배포, 쓰기 활성화, GitHub Actions API URL 변수 설정을 완료했습니다.
- 로컬 Vite는 대형 업무표·간트를 개발 모드에서 두 번 마운트하던 React StrictMode를 제거했습니다. 초기화 요청 자체의 중복 방지 가드는 유지하며 운영과 같은 1회 렌더링 기준으로 로컬 체감을 맞췄습니다.
- 유효한 로그인 세션은 같은 탭에서 재사용합니다. 최소 bootstrap에는 총괄·업무 원문을 포함하지 않으며, 로그인 직후 현재 탭 데이터부터 우선 조회하고 후속 탭 스냅샷은 백그라운드에서 준비합니다.
- 현재 내부 운영 계정의 로그인과 bootstrap에서 Pocket은 UND·무극·포켓컴퍼니 3개 프로젝트, NS는 멤버십이 있는 UND·무극 2개 프로젝트만 받는 것을 실제 Supabase RPC로 검증했습니다. Pocket은 권한 관리 접근 성공, NS는 `forbidden`을 반환하는 것을 교차 검증했습니다. 잘못된 비밀번호는 거부되고 공개 미리보기 요청도 차단됩니다.
- 무극 프로젝트의 2026-08-31 공개 시트 이관 업무 20건은 2026-09-02 초기화에서 제거했습니다. 현재 23건은 기준 캠페인 HTML에서 다시 생성했으며, 원본의 날짜 경과율을 실제 진행률로 쓰지 않고 `progress_percent = 0`으로 이관했습니다. 이후 진행률은 사용자 직접 입력값만 저장합니다.
- 업무 생성·동일 mutation 재호출·수정·stale row version 거부·보관을 실제 원장과 활동로그에서 검증했습니다.
- 업무·프로젝트 착수일·회의록·KPI·고객 권한을 저장하는 동안에는 전체 화면을 어둡게 덮는 중앙 저장 모달을 표시합니다. 서버의 최종 성공·실패 응답 전까지 배경 포인터와 키보드 탐색을 잠가 화면 이동과 중복 저장을 막습니다. 간트의 여러 행처럼 저장 요청이 연속되는 경우에는 이를 하나의 저장 세션으로 합쳐 모달을 계속 유지하고, 마지막 요청이 끝난 뒤 한 번만 해제합니다.
- 성과 탭에서 프로젝트별 KPI 정의를 생성·수정·보관할 수 있습니다. 목표값·단위·측정주기·채널·고객 공개 여부는 `11_KPI정의`에 저장되며, 고객 계정은 조회만 가능합니다. 무극 프로젝트에서 생성→수정→보관 흐름과 보관 후 조회 제외를 실제 배포 API로 검증했습니다.
- 업무 화면은 `일정표 / 간트 / 업무 로그`로 구성합니다. 세 화면은 같은 캠페인 진행률·탭·본문 패널 골격 안에서 내용만 교체하며, 업무 로그 진입 시 별도 헤더나 다른 화면 UI로 전환하지 않습니다. 선택된 화면 탭은 진한 남색 배경·흰 글자·체크 아이콘으로 표시해 비활성 탭과 명확히 구분합니다. 일정표는 `업무 / 세부내용 / 시작일 / 종료일 / 기간(일) / 진행률 / 상태 / 담당 / 완료링크 / 비고` 10개 열을 표시하고, 간트는 같은 업무 행을 월·일·요일 날짜축에 연결합니다. `#tasks/schedule`로 직접 진입할 수 있으며 고객의 `tasks` 권한을 그대로 상속합니다. 기간은 날짜에서 자동 계산하며 나머지는 `06_업무`에 분리 저장합니다. 로그는 내부 사용자가 탭을 열 때만 `15_활동로그`의 확정된 TASK 이벤트를 조회합니다.
- 포켓 계정 전용 `권한 관리` 화면을 추가했습니다. 고객사 로그인 계정을 생성·비활성화하고 프로젝트와 허용 페이지를 지정할 수 있습니다. 계정은 `03_사용자`, 접근 범위는 `04_프로젝트권한.allowed_pages_json`, 비밀번호는 Script Properties의 HMAC digest로 분리 저장합니다. 고객 화면의 메뉴 숨김과 Apps Script의 페이지별 서버 차단을 함께 적용했습니다.
- 권한 관리 백엔드는 `2026-08-27-permission-admin-v20`으로 운영 Web App에 배포했고 health `READY`, 쓰기 활성화를 확인했습니다. 운영 데이터에 임의 고객 계정을 만들지는 않았으며, 실제 첫 계정 생성은 포켓 계정의 권한 관리 화면에서 수행합니다.
- `성과 추적` 화면을 별도로 추가했습니다. `12_성과일별`을 기준으로 노출→반응→클릭→문의 병목, 채널별 비용·문의 기여와 일별 추이를 표시합니다. 별도 JSON이나 프런트 임시 데이터는 저장하지 않습니다.
- 최초 구현이 여러 원장을 한 요청에서 다시 읽어 30초 이상 지연된 문제를 제거했습니다. 성과 추적을 프로젝트 스냅샷에서 분리하고 일별 성과 원장만 읽도록 경량화한 백엔드 `2026-08-27-outcome-tracking-v22`를 운영 Web App에 배포했습니다. 인증된 실제 요청은 약 3초에 정상 응답했으며 현재 UND·무극의 `12_성과일별` 행은 모두 0건입니다.
- 2026-08-31 첫 로딩 진단에서 로그인→bootstrap 2회 순차 Apps Script 실행과 업무 탭의 스냅샷 최대 4초 선대기를 병목으로 확인했습니다. 로그인·bootstrap·첫 총괄/업무 데이터를 한 실행으로 묶고 현재 화면을 우선 표시하도록 구현한 백엔드 식별자는 `2026-08-31-fast-startup-v24`입니다.
- 2026-08-31 무극 일정 동기화에서 Sheets 날짜 셀이 Date 객체로 반환될 때 unrelated 업무 수정까지 `invalid_date`로 거부되던 검증을 수정했습니다. 읽기 응답과 저장값은 KST `yyyy-MM-dd`로 정규화하며 운영 백엔드는 `2026-08-31-muguk-schedule-v25`입니다.
- 업무 바로 아래에 `데일리 회의록` 화면을 추가했습니다. 날짜·참석자·회의 내용·결정사항·후속업무·작성자·공개범위를 `19_데일리회의록` 15개 열에 분리 저장하며 수정 시 행 버전 충돌 보호와 활동로그를 사용합니다. 운영 시트 생성과 빈 목록 조회를 검증했고 백엔드 `2026-08-31-daily-meetings-v26`은 `READY`, 쓰기 활성 상태입니다.
- `06_업무`의 `progress_percent`는 사용자 직접 입력 전용입니다. 이전에 상태 또는 일정 경과율로 자동 입력된 165개 행은 전부 0으로 정리하고 열 서식도 숫자 `0`으로 고정했으며, 완료·미착수 상태 변경도 진행률을 자동으로 덮어쓰지 않습니다. 일회성 초기화 API는 실행 직후 운영 라우터에서 제거했습니다. `completion_url`, `remarks`, 무극 HTTPS 완료링크 3건과 비고 10건은 그대로 유지합니다. 운영 백엔드는 `2026-08-31-manual-progress-v30`입니다.

### 2026-08-31 운영 안정화 v31

- 운영 백엔드는 `2026-08-31-stability-v31`, 현재 Web App 배포는 @87입니다.
- 저장 요청은 같은 `mutation_id`를 한 번 재시도하고, `20_뮤테이션`에서 확정 결과를 조회합니다. 기존 활동로그 109개 mutation을 인덱스로 이관했으며 PREPARE/COMMIT/FAILED 상태를 별도 행으로 유지합니다.
- 저장 성공 화면은 대상 mutation 응답만 기다립니다. 활동로그 재조회는 백그라운드로 이동해 저장 버튼 체감 지연을 줄였습니다.
- `21_백업로그`를 추가하고 GitHub Actions가 매일 03:00 KST에 `scheduled_backup`을 호출합니다. 러너 비밀값은 GitHub Actions Secret과 Script Properties의 digest로 분리 저장합니다.
- 실제 백업 `PocketMarketingHub_20260831_183904`을 Google Drive의 별도 스프레드시트로 생성하고 필수 시트 누락 0건으로 열기 검증했습니다. 동일 날짜 재호출은 중복 백업을 건너뜁니다.
- `07_업무의존성` 구버전 열은 새 방향성 열을 추가해 호환했고, `16_동기화상태.sync_id`는 `sync_status_id`로 복제했습니다. 기존 열과 행은 삭제하지 않았습니다.
- 전체 `deep_health` 기준의 헤더·기본키·활성 권한·고객사/프로젝트 범위 검사를 통과했습니다.
- GitHub Pages CI는 Sites 패키징 결과를 먼저 생성하고, 프런트·Apps Script 계약 테스트 60개가 모두 통과한 경우에만 Pages에 배포합니다.
- Apps Script 콜드 스타트가 12초를 넘길 때 정상 응답까지 프런트가 먼저 취소하던 문제를 수정했습니다. 로그인은 최대 30초, 일반 조회는 최대 20초를 허용하고 동일 탭의 최근 bootstrap은 즉시 표시합니다.
- GitHub Pages 운영 URL을 고객 조회 계정으로 헤드리스 검증했습니다. 총괄·실행계획·업무·콘텐츠·성과, 고객사 전환, 모바일 메뉴·가로 넘침, 콘솔 오류 검사를 모두 통과했고 첫 총괄 표시는 실제 측정에서 약 9~14초였습니다.
- Pages 배포와 일일 백업 GitHub Actions를 각각 수동 실행해 성공을 확인했습니다. 백업 작업은 Apps Script의 Google 리다이렉트를 추적하도록 고정했습니다.

### 2026-09-01 저장·백업·권한 격리 v33

- 운영 백엔드는 `2026-09-01-permission-isolation-v33`, Web App 배포는 @89입니다. health와 관리자 `deep_health`는 `READY`, `schema_audit`는 유효 상태를 반환했습니다.
- 완료 상태 진입 시 `completed_at`을 기록하고, 완료 상태 유지 수정에서는 보존하며, 완료 상태를 벗어나면 비웁니다. 화면 표시용 계산 마감일은 더 이상 사용자가 입력한 `due_date`로 저장되지 않습니다.
- 저장 성공과 PREPARE 복구 성공 시 프로젝트별 고객 읽기 캐시 epoch를 변경합니다. 운영 임시 업무로 고객 조회 즉시 반영, 완료 시각 기록·해제, 보관 후 즉시 제외를 실제 API에서 확인했습니다.
- `20_뮤테이션` 운영 감사 결과 전체 113건이 COMMIT이며 PREPARE 0건, FAILED 0건, 15분 이상 미확정 항목 0건입니다.
- 일일 백업은 21개 전체 시트의 표시값 행렬을 SHA-256 manifest로 기록하고 복제본과 재계산 비교합니다. 최신 `PocketMarketingHub_20260901_011142`는 21/21 일치, 누락 0, 불일치 0으로 검증했습니다.
- 권한 감사에서 UND 고객 계정에 무극 읽기 권한이 함께 연결된 오류를 발견했습니다. `MEM-MUGUK-USR-UND`만 보관 처리하고 UND 프로젝트 권한은 유지했습니다. 재로그인 결과 UND bootstrap은 `PRJ-UND-90D-001` 한 건만 반환하며 무극 직접 조회와 내부 실행계획 조회는 모두 `forbidden`입니다. NS의 권한 관리 접근도 `forbidden`으로 재검증했습니다.
- 권한 관리 화면과 API는 계정을 비활성화하지 않고 특정 프로젝트 멤버십만 제거할 수 있습니다. 멤버십 ID와 프로젝트 ID가 같은 활성 권한 행을 가리킬 때만 처리합니다.

### 2026-09-01 UND 업무 구조 통일 v37

- 운영 백엔드는 `2026-09-01-und-task-structure-v37`, Web App 배포는 @93입니다. health는 `READY`, 쓰기와 백업 설정은 활성 상태입니다.
- 무극과 같은 9열 일정표 구조를 UND 승인 업무 144건에도 적용했습니다. `세부내용`, `planned_start_date`, `due_date`는 144건 모두 명시적으로 저장했고 `remarks`는 승인 계획의 주석이 있는 47건만 채웠습니다.
- 프로젝트 기간은 `2026-08-25~2026-12-15`이며 P0/M1/M2/M3와 `plan_week`를 기준으로 업무별 실행 구간을 배치했습니다. 기존 상태·담당·완료값은 보존했고 사용자가 입력하지 않은 진행률은 0으로 유지했습니다.
- 고객 계정에도 공개 업무의 `description`을 제공해 일정표의 세부내용이 비지 않도록 했습니다. `plan_note`, 검토 조직, 차단 사유 등 내부 필드는 계속 제외합니다.
- Sheets Date가 서버 캐시에 UTC ISO 문자열로 직렬화될 때 한국 날짜가 하루 전으로 잘리던 공통 오류를 수정했습니다. 캐시 전·후 2회 조회에서 프로젝트 기간, 144개 일정, 세부내용이 동일함을 확인했습니다.

### 2026-09-01 최근 업데이트 카드 정리 v38

- 운영 백엔드는 `2026-09-01-activity-card-v38`입니다.
- 구조 이관용 `TASK_PLAN` 시스템 로그는 총괄의 최근 업데이트 4건에서 제외합니다. 전체 원장 로그는 삭제하지 않아 운영 감사 근거는 유지합니다.
- 활동 요약의 한국어 조사를 엔터티별로 고정해 `항목가 변경됨` 같은 잘못된 문구가 노출되지 않도록 했습니다. 마이그레이션 로그를 별도 조회할 때는 `업무 계획이 동기화됨`으로 표시합니다.
- 최근 업데이트 행은 아이콘·본문 열의 최소 폭, 긴 문자열 줄바꿈, 행 구분선과 카드 오버플로를 명시해 좁은 화면에서도 항목이 경계 밖으로 밀리지 않게 했습니다.
- 데일리 회의록은 포켓·NS 사용자가 웹의 `회의록 작성` 버튼에서 입력하고 수정합니다. Google Sheets는 화면 뒤의 영속 저장소로만 사용하며 회의록 화면에는 시트 입력을 요구하는 문구를 노출하지 않습니다. 작성자와 작성 시각은 로그인 세션과 서버 시각으로 자동 기록합니다.
- 업무 일정표는 스프레드시트형 전면 격자 대신 업무별 카드 행과 하나로 이어진 간트 블록을 사용합니다. 기본 화면은 업무·기간·상태·일정 블록에 집중하고 세부내용·진행률·결과물·비고는 `상세 보기`에서만 노출합니다. 저장 필드와 일정 계산은 변경하지 않습니다.

### 2026-09-01 페이지 권한 카탈로그 정리 v39

- 좌측 운영 메뉴와 고객 계정 생성의 페이지 선택지를 `frontend/src/accessPermissions.js`의 단일 `PAGE_CATALOG`에서 파생합니다. 새 화면을 추가할 때 메뉴만 늘고 고객 권한 선택지가 누락되는 구조를 제거했습니다.
- 현재 고객 권한 선택지는 `총괄 현황 / 실행계획 / 업무 / 데일리 회의록 / 성과 / 세부 로그` 6개입니다. 업무 권한은 동일한 `06_업무` 원천을 사용하는 업무 목록·일정표·업무 로그를 함께 제어하고, 데일리 회의록은 별도 서버 권한으로 분리했습니다.
- `콘텐츠`와 `성과 추적`은 데이터·API를 삭제하지 않고 운영 메뉴와 신규 고객 권한 선택에서 숨겼습니다. 고객 직접 API 조회도 허용 목록 정규화 단계에서 차단합니다.
- 기존 `자료·활동`은 `세부 로그`로 변경했습니다. 자료 목록과 자료 등록 버튼을 제거하고 `15_활동로그`의 확정 이력만 조회하며, 화면 진입 시 불필요한 파일 API 요청도 보내지 않습니다.
- 화면 추가·숨김 변경은 프런트 카탈로그, Apps Script `MH_ACCESS_PAGES`, 권한 계약 테스트, `DATA_MODEL.md`를 한 변경 단위로 갱신하는 것을 배포 조건으로 고정했습니다.

### 2026-09-01 프로젝트 일정표 필터

- 프로젝트 일정표에 드롭다운 대신 바로 누르는 박스형 `업무 상태 / 업무 카테고리 / 업무 일정` 필터와 결과 건수·초기화 기능을 추가했습니다. 세 조건은 동시에 적용됩니다.
- 상태는 `완료 / 진행 / 보류`, 카테고리는 `마케팅 / 디자인 / 영상`, 일정은 월요일~일요일 기준 `지난주 / 이번주 / 다음주`로 구분합니다. 업무 기간이 선택한 주와 하루라도 겹치면 표시하며 사용자가 직접 입력하지 않은 진행률은 필터 근거로 사용하지 않습니다.
- 브라우저의 오늘 날짜가 JavaScript `Date` 객체일 때 주간 필터가 0건으로 계산되던 날짜 정규화 오류를 수정하고 실제 화면 입력 형태를 회귀 테스트에 추가했습니다.
- 일정표 행은 한때 `시작일 → 종료일 → sort_order`로 정렬했으나 매체가 흩어져 기준 HTML과 달라지는 문제가 있어, 현재는 문서 상단의 `매체 그룹 → 그룹 내부 원장 순서` 규칙으로 대체했습니다. 원장 정렬값 자체는 변경하지 않습니다.
- 필터는 브라우저 표시 전용이며 Google Sheets 값을 수정하거나 별도 필터 상태를 원장에 저장하지 않습니다.

### 2026-09-01 첫 화면 탐색 기본값

- 데스크톱 로그인 직후 접근 가능한 회사 목록은 상단 이름형 탭에 표시하고, 왼쪽 한 열에는 현재 프로젝트의 화면 메뉴만 표시합니다. 포켓은 UND·무극·포켓컴퍼니를 전환하고 NS는 권한이 있는 UND·무극만 전환합니다.
- 데스크톱의 단일 탐색 버튼과 3단계 순환은 제거했습니다. 모바일은 본문을 가리지 않도록 일반 메뉴 아이콘으로 통합 서랍을 사용합니다.

## 현재 데이터 경계

| 구분 | 데이터 원천 | 영속 저장 | 상태 |
|---|---|---:|---|
| 운영 웹 화면 | Supabase RLS/RPC + 미전환 화면의 Apps Script 응답 | 해당 없음 | 단계적 실연동 |
| Google Sheets 기준본 | 기존 사용자 입력·승인 이관 및 미전환 데이터 | 예 | 검증·복구 기준본 + 미전환 원장 |
| 업무·회의록·KPI·권한 | Supabase | 예 | 원장+활동로그 동시 기록 |
| 콘텐츠·자료 생성 | 포켓·NS 웹 입력 → Google Sheets | 예 | 미전환 원장+활동로그 동시 기록 |
| 고객사 화면 | 본인 프로젝트의 CLIENT 공개 데이터 | 해당 없음 | 읽기 전용 |
| 실행계획 | Supabase `plans` + `plan_sections` | 예 | 공유용 10개·내부용 21개 섹션 직접 조회; Sheets는 기준본 |
| 성과 추적 | 12 집계 (`CLIENT_VIEWER`는 05 공개 채널 확인) | 아니요(읽기 전용) | 약 3초 응답 검증; 현재 UND·무극 원장 행 0건 |
| 채널 성과 동기화 | 아직 미설정 | 아니요 | 별도 연결 필요 |
| 활동로그 | 웹 mutation PREPARE·COMMIT | 예 | 관리자/실행사 조회; 업무 화면은 COMMIT TASK 이력만 투영 |
| mutation 인덱스 | 20_뮤테이션 | 예 | 중복 요청의 확정 결과를 활동로그 전체 스캔 없이 조회 |
| 백업 이력 | 21_백업로그 + Drive 복제본 | 예 | 매일 03:00 KST 자동 호출; 21개 시트 manifest 해시 검증 완료 |

## 원장에 준비된 범위

원장은 다음 관계를 불변 ID로 연결하도록 설계돼 있습니다.

`고객사 → 프로젝트 → 업무·콘텐츠 → 승인·성과 → 파일·활동로그`

승인된 UND 업무 144건은 운영 집계에 포함됩니다. 기존 103건에서 144건으로 이관할 때 의미상 동일 업무는 상태·담당자·메모를 보존하고 누락 업무만 추가하도록 마이그레이션을 구성했습니다. 콘텐츠, 승인, 성과, KPI 실적은 운영 입력을 받는 원장입니다. 외부 채널 API는 아직 연결되지 않았습니다.

업무 144건의 구성은 다음과 같습니다.

| 구분 | 건수 |
|---|---:|
| P0 / M1 / M2 / M3 | 57 / 31 / 28 / 28 |
| 마케팅 / 디자인 / 영상 | 79 / 35 / 30 |

업무 API는 프로젝트 `start_date`, 업무 `plan_week`, 명시적인 `planned_start_date`·`due_date`를 반환합니다. UND 144건과 무극 20건은 모두 두 날짜를 저장하며, 프런트의 계산 경로는 구버전 무일정 행을 위한 호환용입니다. `plan_note`는 내부 상세에서만 조회되며 고객 화면은 `description`, `contract_linked` 여부와 공개 진행 메모를 확인합니다.

세부 탭과 필드 정의는 [데이터 모델](DATA_MODEL.md), 화면별 읽기 구조는 [API 계약](API_CONTRACT.md)을 기준으로 합니다.

### 2026-09-02 프로젝트 업무 프레임 통일

- UND와 무극의 업무 목록·일정표·업무 로그는 같은 React 컴포넌트와 같은 생성·수정 API를 사용합니다. 프로젝트별 조건문으로 버튼이나 필터를 숨기지 않습니다.
- 포켓·NS 내부 계정의 일정표에는 모든 접근 프로젝트에서 `업무 추가`와 일정 블록 수정 진입점을 동일하게 노출합니다. 서버는 저장 때마다 실제 프로젝트 권한을 재검증하므로 프런트 노출이 권한 우회를 만들지는 않습니다.
- bootstrap 브라우저 캐시를 v2로 갱신해 이전 프로젝트 권한 셸을 폐기했습니다. 축약 업무 응답은 bootstrap 프로젝트 셸과 병합해 고객사명·프로젝트명·권한 정보가 화면 전환 중 유실되지 않습니다.
- 담당 조직의 고객사 라벨에서 `UND` 고정값을 제거했습니다. `CLIENT` 코드는 선택 프로젝트의 고객사명으로 표시됩니다.
- 업무 로그는 현재 업무 원장과 ID를 대조해 업무명을 복원하고, `03_사용자`와 매칭된 계정의 `CREATED / UPDATED / ARCHIVED`만 표시합니다. 내부 ID·자동화·시스템·마이그레이션·변경값 없는 수정·기술 필드만 바뀐 이벤트는 제외하며, 원본은 삭제하지 않고 세부 로그에서 내부 ID·처리자와 함께 조회합니다.
- 일정표 날짜축은 실제 표시 업무의 가장 늦은 종료일에서 끝냅니다. 프로젝트 종료일까지 업무가 없는 빈 날짜 열을 만들지 않으며, 업무 일정이 전혀 없을 때만 프로젝트 기간을 예비 범위로 사용합니다.
- 좌측 탐색은 `고객사 레일 / 프로젝트 메뉴 / 본문`의 3단 구조를 유지하되, 고객사 레일은 옅은 회청색, 프로젝트 메뉴는 흰색, 본문은 회색 바탕으로 명도를 분리했습니다. 두 탐색 열의 오른쪽에는 고정 세로 경계선을 두고 프로젝트 메뉴 하단에도 구분선을 넣어 서로 붙어 보이지 않게 합니다.
- 업무 탭의 마지막 정상 응답은 로그인 사용자·프로젝트별로 현재 브라우저 탭의 `sessionStorage`에 30분 보관합니다. 재방문·업무/일정표 전환 때 저장된 업무표를 즉시 그리고 Google Sheets 최신값을 백그라운드로 다시 읽으므로 Apps Script 응답 동안 전체 표가 로딩 화면으로 가려지지 않습니다. 로그아웃하면 이 화면 캐시도 제거합니다.
- 로컬 미배포 시안은 참고용 `캠페인 스케줄 관리.html`을 업무 화면의 기본 구조로 사용합니다. 렌더링 순서는 `전역 캠페인 헤더 / 전체 진행률·상태 집계 / 일정표·간트·필터 툴바 / 업무 그리드`이며 기존 카드형 총괄과 업무 ViewHeader를 이 위에 겹치지 않습니다. 같은 `06_업무`를 단계 카드와 12열 표로 다시 표시하던 `운영 보드`는 UI와 활성 컴포넌트에서 제거했습니다. 해당 화면에만 있던 `완료 업무 추가`는 일정표 헤더로, 포켓 전용 `착수일 수정`은 필터 툴바로 이동했습니다. 날짜로 진행률을 추정하거나 브라우저 로컬스토리지에 별도 저장하지 않으며 기존 조회·생성·수정 API와 권한·활동로그 계약은 유지합니다. 이 시안은 아직 GitHub Pages에 배포하지 않았습니다.
- 일정 화면의 외형도 참고 HTML에 맞춰 흰색 평면 패널, 짙은 남색 보기 전환, 파란색 활성 필터, 조밀한 격자형 업무 행, 매체·상태별 색상 레일과 직선형 간트 블록으로 교체했습니다. `일정표`는 10개 업무 정보 열을 우선 표시하고 `간트`는 같은 행의 날짜축을 표시하므로 두 화면의 데이터가 갈라지지 않습니다. 업무 생성·수정 버튼과 서버 저장 동작은 기존 컴포넌트를 재사용합니다.
- 같은 로컬 시안에서 참고 HTML의 업무 본문 구성을 앱 셸에 적용했습니다. 데스크톱은 예전 구조대로 왼쪽 `UND·무극 고객사 목록 / 현재 프로젝트 메뉴`를 고정 표시하고, 메뉴를 바꾸는 좌우 화살표·프로젝트 팝업은 렌더링하지 않습니다. 프로젝트 메뉴에서 `총괄 현황 / 실행계획 / 업무 / 일정표 / 데일리 회의록 / 성과 / 세부 로그 / 권한 관리`로 바로 이동합니다. 실행계획의 클라이언트 공유용·내부용 하위 경로, 고객 계정의 허용 페이지 필터, 포켓 전용 권한 관리 제한은 기존 계약을 유지하며 아직 GitHub Pages에는 배포하지 않았습니다.
- 로컬 간트에는 참고 HTML의 포인터 페인팅과 표시 코드를 함께 이식했습니다. `g-hrow / g-months / g-days / g-grow / g-row / g-track / g-c` DOM과 280px 고정 업무 열, 28px 날짜 셀, 32px 업무 행, 분야 밴드, 오늘 기준선을 원본과 동일하게 사용합니다. 빈 셀 시작은 추가, 채워진 셀 시작은 삭제이고 여러 업무·여러 날짜를 한 번에 사각형 선택합니다. 비연속 날짜를 보존하기 위해 `06_업무.schedule_dates_json`을 추가했으며 서버가 선택 날짜의 최솟값·최댓값을 시작일·종료일에 동기화합니다. 구버전 빈 값은 기존 연속 구간과 호환되고 `[]`는 전체 일정 삭제입니다. 현재 프런트·Apps Script 소스만 수정된 미배포 상태이므로 운영 반영 전 `mhSetupEnsureTaskTableFields()` 실행과 새 Web App 배포가 필요합니다.
- 2026-09-02 간트 화면은 업무를 분야별로 묶어 표시하면서 드래그 계산은 날짜순 원본 배열을 사용하던 행 인덱스 불일치를 수정했습니다. 화면에 렌더링된 분야·업무 순서를 드래그 미리보기와 UPDATE 저장의 단일 기준으로 사용하므로 한 업무를 편집할 때 다른 업무까지 함께 변경되는 경로를 차단했습니다. 회귀 테스트를 추가했으며 로컬 소스만 수정한 미배포 상태입니다.
- 같은 날 `캠페인 스케줄 관리.html`의 간트 외곽까지 다시 대조해 `toolbar → panel-head → g-hint → scroll → gantt → g-legend` 계층, 8px 패널 모서리, 원본 그림자·테두리·간격, 보기 전환 세그먼트와 필터 칩, `매체 · 업무` 머리글을 기준 HTML 수치로 통일했습니다. 기존에 별도 캠페인 스킨이 덮어쓰던 10px 카드와 흰색 헤더는 제거했습니다. 프로젝트 필터·업무 추가·수정·Sheets 저장은 기능 연결이므로 유지하며 이 변경 역시 로컬 미배포 상태입니다.
- 후속 대조에서 간트의 `g-c`에 구형 일정표용 `task-schedule-cell` 클래스가 함께 붙어 뒤쪽 CSS가 원본 행 높이와 셀 배경을 다시 덮는 문제를 확인했습니다. 간트 셀을 기준 HTML의 `g-c / on / rs / re` 구조로 분리하고, 그룹 기준도 업무 분야가 아닌 원본 매체와 `sort_order` 순서로 복구했습니다. 업무가 존재할 때 날짜축은 프로젝트 계약 기간이 아니라 실제 첫 업무일~마지막 업무일만 사용합니다. 헤드리스 브라우저에서 셀 28×32px, 행 33px(32px 내용+1px 경계), 고정 업무 열 280px, YouTube 매체 그룹, 일정표↔간트↔업무 로그 전환을 확인했습니다. 이 변경은 로컬에만 반영했으며 GitHub Pages에는 아직 배포하지 않았습니다.
- 업무 로그 진입 시 일정표 컴포넌트와 함께 보기 전환 탭까지 사라지던 조건부 렌더링을 제거했습니다. `일정표 / 간트 / 업무 로그` 선택 상태를 업무 화면 상위에서 관리하고 로그 화면에도 같은 탭을 고정해 세 화면을 양방향으로 이동할 수 있습니다. 로그를 보고 돌아가도 직전에 선택한 일정표·간트 보기를 유지하며, 프로젝트를 바꾸면 새 프로젝트의 일정표로 안전하게 초기화합니다. 고객 계정에는 기존 권한 계약대로 업무 로그를 노출하지 않습니다.
- 내비게이션 회귀 검수는 업무 0건 상태, 포켓 계정의 `일정표 → 간트 → 업무 로그 → 일정표·간트` 왕복, UND·무극 프로젝트 전환, 고객 계정의 로그 비노출, 390px 모바일 새로고침과 페이지 가로 넘침까지 헤드리스 브라우저로 확인했습니다. 정적·계약 테스트 104건과 프로덕션 빌드도 통과했으며 이 변경은 아직 GitHub Pages에 배포하지 않았습니다.
- 2026-09-02 데스크톱 전역 내비게이션을 다시 고정형으로 정리했습니다. 고객사 레일과 프로젝트 메뉴가 항상 왼쪽에 함께 표시되며 `<`·`>` 버튼으로 탐색 단계를 바꾸지 않습니다. 900px 이하에서만 일반 메뉴 아이콘으로 두 열을 한 서랍에 표시합니다. 업무 화면·프로젝트 전환·모바일 서랍을 포함한 헤드리스 검수와 정적·계약 테스트 105건, 프로덕션 빌드를 통과했으며 로컬에만 반영했습니다.

## 공개 범위

원장과 API 공개 범위는 `POCKET_ONLY`, `PROJECT_TEAM`, `CLIENT`로 통일했습니다. 기존 `INTERNAL` 값은 `POCKET_ONLY`로 이관했고, 신규 실행사 입력의 기본 공개 범위는 `PROJECT_TEAM`입니다.

권장 기준은 다음 세 단계입니다.

| 코드 | 공개 대상 | 포함 예시 |
|---|---|---|
| `POCKET_ONLY` | 포켓 내부 | 전략, 원가, 위험, 계약 기준, 내부 메모 |
| `PROJECT_TEAM` | 포켓 + 배정 실행사 | 제작 지시, 작업 메모, 실행사 담당 정보 |
| `CLIENT` | 고객사 | 일정, 공개 결과물, 승인 요청, 공개 성과 |

고객 공개는 포켓 검수 후 `CLIENT`로 변경합니다.

## 운영 구조

```text
GitHub Pages 프런트엔드
        ↓ HTTPS
Apps Script API
        ├─ 로그인 세션 확인
        ├─ 사용자·프로젝트 권한 확인
        ├─ 역할별 응답 필드 화이트리스트
        ├─ 입력 검증·동시 저장 제어
        └─ 활동로그 기록
        ↓
비공개 Google Sheets 원장
```

브라우저가 보낸 `role`, `client_id`, `project_id`는 권한 근거로 신뢰하지 않습니다. 서버가 로그인 사용자와 프로젝트 권한표를 기준으로 접근 범위를 다시 계산해야 합니다. 시트 원본은 비공개로 유지하고 고객에게 직접 공유하지 않습니다.

## 읽기 연동 상태

사용자·권한 등록, 공개 범위, 통합 bootstrap, 화면별 지연 조회, 보관행 제외, 역할별 필드 projection, 로딩·오류·재시도·마지막 동기화 표시를 구현했습니다. 활성 고객사·프로젝트는 UND·무극·포켓컴퍼니이며, 포켓 운영 계정은 세 프로젝트를 보고 NS는 멤버십이 있는 UND·무극만 봅니다. 고객용 `CLIENT_VIEWER` projection은 코드상 유지되지만 현재 공개 세션 발급이 꺼져 있어 로그인 없이 접근할 수 없습니다.

## 쓰기 연동 상태

2026-09-02부터 프로젝트 일정표의 달력 구간 블록을 클릭하면 블록 안에 `수정` 버튼이 나타나고, 이를 눌러 업무 수정 모달을 엽니다. 상태·업무명·시작일·종료일·진행률·우선순위·담당·세부내용·완료링크·비고를 업무 목록과 같은 `task UPDATE` 경로로 저장하며, 서버 성공 응답과 활동로그 기록 규칙도 동일합니다. 고객 읽기 전용 계정에는 편집 버튼이 표시되지 않습니다. 업무명 셀은 클릭형 컨트롤과 분리해 제목 폭이 색상 막대 규칙에 의해 축소되지 않도록 유지합니다.

같은 날 일정표 헤더에도 쓰기 권한 계정용 `업무 추가` 버튼을 배치했습니다. 기존 생성 폼과 `task CREATE` 경로를 그대로 사용하며, 업무 목록과 일정표 양쪽에 중복 버튼이 나타나지 않도록 화면별 진입 위치를 분리했습니다.

업무·콘텐츠·자료와 KPI 정의의 CREATE 폼 및 서버 create/update/archive 계약은 연결됐습니다. `LockService`, `row_version`, actor에 바인딩된 `mutation_id`, PREPARE·COMMIT 활동로그, 보관 처리를 구현했습니다. 업무 UPDATE는 클릭 즉시 화면 캐시를 변경하고 서버가 반환한 최신 행과 `row_version`으로 확정하며, 실패하면 이전 상태로 롤백합니다. 콘텐츠·자료 CREATE, KPI 정의 변경과 프로젝트 착수일 UPDATE는 저장 성공 후 화면 캐시를 다시 읽습니다. 모든 쓰기는 로그인 세션과 서버 권한 검사를 통과해야 합니다.

### 2026-08-26 전면 저장 감사

운영 데이터를 추가하지 않는 읽기 전용 점검으로 현재 배포와 원장을 대조했습니다.

- 운영 API: `READY`, `writesEnabled=true`, backend `2026-08-26-login-alias-v17`, schema `2026-08-26-v3`
- `06_업무`: 144행, 업무 ID 중복 0건, 필수 필드·`row_version` 누락 0건
- 최근 실저장 확인: `TSK-UND-P0-MKT-12`가 2026-08-26 16:26:25에 `IN_PROGRESS`, `row_version=9`로 저장되고 1초 뒤 `15_활동로그`에 `TASK UPDATED`가 기록됨
- `03_사용자` 첫 헤더가 `1열`로 변형돼 토큰의 `user_id`가 비던 문제를 `user_id`로 복구했고, 두 내부 계정의 로그인과 bootstrap을 재검증함
- 프런트 API·탐색·배포 테스트 22건 및 Apps Script 정적 인증 검사 통과

| 화면 기능 | 대상 원장 | 현재 저장 상태 |
|---|---|---|
| 업무 생성 | `06_업무` | 저장 연결됨; 생성 응답 유실 후 재시도 시 중복 생성 방지 보완 필요 |
| 업무 상태·제목·메모·담당 조직·마감·우선순위 수정 | `06_업무` | 저장 연결됨; 담당 조직은 포켓·NS·UND 3개 코드로 저장 |
| 프로젝트 착수일 | `02_프로젝트` | 내부 역할 전용 UPDATE 연결됨 |
| 콘텐츠 등록 | `08_콘텐츠` | 인증된 내부 계정 CREATE 연결됨 |
| 자료 링크 등록 | `14_파일링크` | 인증된 내부 계정 CREATE 연결됨 |
| 실행계획·성과 | `17_실행계획`·`18_실행계획섹션`·`11_KPI정의`·`12_성과일별`·`13_KPI실적` | 실행계획과 실적은 조회 전용, KPI 정의는 내부 계정 생성·수정·보관 연결됨 |
| 변경 이력 | `15_활동로그` | 웹 mutation마다 PREPARE·COMMIT 기록; 업무 탭에서 COMMIT 이력 조회 |

감사에서 확인된 항목 중 상태 전이, 고객 캐시 무효화, 계산 마감일 역저장, 완료 시각 기록, 동일 mutation 재시도는 2026-09-01까지 해결했습니다. 남은 항목은 다음과 같습니다.

- 공개 업무 저장 응답은 `description`, `assignee_user_id`를 제거해 저장 직후 화면에서 메모·담당자가 빈 값처럼 보이거나 다음 저장에서 덮일 수 있습니다. 현재 익명 공개 쓰기는 꺼져 있지만 고객 계정 쓰기를 다시 열 때 선행 보완이 필요합니다.
- 동일 `mutation_id` 재호출은 중복을 막지만 응답 유실 뒤 사용자가 신규등록을 다시 누르면 새 ID가 발급돼 중복 행이 생길 수 있습니다.
- Sheet 직접 정렬·삽입과 저장이 겹치면 저장 대상 행이 어긋날 수 있습니다. 행 UPDATE 자체는 한 번의 `setValues`로 묶고 기존 수식 문자열을 보존하도록 보완했습니다.
- PREPARE만 남은 거래를 운영 화면에서 별도로 확인하는 관리 UI는 아직 없습니다. 서버 인덱스와 로그에는 상태가 남습니다.
- 업무 조회 상한은 200건이며 콘텐츠·자료도 다음 페이지를 불러오는 화면이 없습니다.
- 자동 일일 백업과 전체 시트 해시 검증은 구현했습니다. 특정 시점 복원본을 운영 원장으로 승격하는 파괴적 복원 훈련은 아직 수행하지 않았습니다.

이 항목들이 해결되기 전까지 저장 상태는 `기본 저장 동작 확인 / 운영 안정화 필요`로 판정합니다.

구체적인 저장 규칙은 [저장·연동 원칙](STORAGE_AND_SYNC.md), 인증과 비밀값 기준은 [보안 경계](SECURITY_BOUNDARY.md)를 따릅니다.

### 2026-09-02 상호작용 성능 최적화 v42 (로컬 미배포)

- `#tasks/schedule`로 새로고침할 때 서버 초기 화면을 `tasks`로 정규화해 업무 데이터가 비어 있는 채 로딩이 끝나지 않던 경로를 수정했습니다.
- 업무 생성·수정은 서버의 최종 행을 현재 화면과 캐시에 바로 병합합니다. 저장할 때마다 업무 전체 목록을 다시 읽거나 관련 없는 실행계획·회의록·성과 캐시를 폐기하지 않습니다.
- 업무·실행계획·회의록·성과의 최근 정상 응답은 사용자별 세션 캐시에 보관하고, 저장 직렬화는 브라우저 유휴 시간에 같은 키의 마지막 값만 기록합니다.
- 화면별 조회와 저장 중에는 전역 연결 상태를 반복 방출하지 않아 대형 업무표와 간트가 함께 다시 그려지지 않습니다. 간트 계산을 메모화하고 드래그 갱신 시 행별 날짜 집합을 한 번만 만듭니다.
- 간트 다중 행 저장은 최대 40건을 `mutate_batch` 한 요청으로 묶습니다. 청크당 인증·권한·`ScriptLock`·캐시 epoch 갱신을 한 번만 수행하고, PREPARE/COMMIT/뮤테이션 색인은 배열 추가, 연속 업무 행은 범위 `setValues()`로 합칩니다. 행별 `mutation_id`와 `row_version`은 유지하며 v43 미배포 서버에서는 동일 ID의 단건 순차 저장으로 자동 호환됩니다. 프런트와 Apps Script v43 소스만 수정된 미배포 상태입니다.
- 단건 mutation도 PREPARE와 COMMIT에서 동일한 활동로그 테이블 참조를 재사용해 PREPARE 이후 `15_활동로그` 전체를 다시 읽던 증가형 지연을 제거했습니다.
- Apps Script는 인증된 모든 역할의 완성 응답 캐시를 사용합니다. PREPARE는 `15_활동로그`에 먼저 확정하되 중복 뮤테이션 색인 쓰기를 제거하고, 최종 COMMIT/FAILED만 `20_뮤테이션`에 남깁니다. 단건 추가는 `setValues` 한 번으로 통일했습니다.
- 로컬 개발의 React StrictMode 이중 마운트를 제거하고 React·아이콘을 별도 번들로 분리했습니다.
- 자동 테스트 108건과 프로덕션 빌드를 통과했습니다. UND 78건·무극 23건의 합성 데이터로 일정표 → 간트 → 업무 로그 → 간트/로그 → 일정표, 고객사 전환, 모바일 재진입까지 헤드리스 탐색 검수를 통과했습니다.

현재 운영은 Web App @101의 v41이며, 이 절의 v42와 프런트 변경은 아직 GitHub Pages·Apps Script 어느 쪽에도 배포하지 않았습니다.

### 2026-09-03 신규 업무 인지 v44 (로컬 미배포)

- `tasks` 응답에 원장 `created_at`을 포함하고 프런트가 생성 후 24시간 동안만 신규 업무로 판정합니다. 수정 시각과 일정 날짜는 판정에 사용하지 않아 기존 업무 수정이 신규로 다시 표시되지 않습니다.
- 일정표 행과 간트 행에 옅은 강조색과 `신규` 배지를 함께 표시합니다. 공통 상단바 우측 알림 버튼에서 24시간 내 신규 업무 전체와 미확인 건수를 다시 열어볼 수 있고, 업무 선택 또는 `모두 확인` 시 확인한 업무 ID를 프로젝트별 세션에 저장합니다. 이미 불러온 업무 응답만 사용하므로 알림을 위한 추가 Sheets 요청은 없습니다.
- 신규 판정은 읽기 응답에서 파생하므로 원장 열 추가나 추가 쓰기가 없습니다. v44 Apps Script와 프런트는 아직 운영 Web App·GitHub Pages에 배포하지 않았습니다.
- 운영 API가 아직 v41인 상태에서 간트 한 행 변경도 미지원 `mutate_batch`를 먼저 탐색한 뒤 단건 `mutate`를 다시 보내던 이중 왕복을 제거했습니다. 한 행은 즉시 단건 저장하고, 다건 배치 미지원이 한 번 확인되면 같은 API 세션에서 실패 탐색을 반복하지 않습니다.
- 간트는 변경한 날짜 경계와 일별 배열 세 필드만 전송합니다. 이전처럼 제목·상태·담당·설명 등 업무 전체를 다시 보내지 않아 요청 크기와 불필요한 검증 범위를 줄였습니다.
- 일반 저장 실패는 간트의 낙관적 화면만 원복하고 로컬 오류로 표시합니다. 세션 만료가 아닌 저장 오류를 전역 프로젝트 읽기 실패로 방송하던 경로를 제거해 정상 업무 원장이 오류 화면으로 교체되지 않게 했습니다.

## 구현 우선순위

### 완료

- 공개 범위 통일과 참고 데이터 보관
- 사용자·프로젝트 권한 등록
- 서버 인증·고객사 격리·역할별 projection
- 화면별 읽기와 신규등록 UI
- `mutation_id`, `row_version`, `LockService`, 활동로그
- 로딩·오류·재시도·저장 상태

### 완료된 운영 검증

- Apps Script 최초 OAuth 승인과 health `READY`
- 3개 역할 로그인·프로젝트 격리·고객 쓰기 차단
- 신규등록·중복 mutation·stale row version·보관 흐름
- 원장 행과 PREPARE·COMMIT 활동로그 대조
- 접근코드·서명키 회전 및 Script Properties 동기화

### P2 — 성과와 외부 채널

- 유튜브·메타·네이버·구글 데이터 수집
- 원천 성과와 KPI 집계 분리
- 동기화 상태, 마지막 성공 시각, 실패 사유 표시

### P3 — 남은 운영 안정성

- 백업 파일의 30일 자동 정리(현재는 Drive 루트에 누적)
- 격리된 테스트 원장으로의 실제 복원 훈련과 승격 체크리스트
- 저장·동기화 오류의 외부 알림 채널
- 권한 격리·보관·복원 E2E 자동 테스트 확대

## 운영 연결 완료 판정

다음 조건을 모두 만족하기 전에는 화면에 `Google Sheets 연동 완료`를 표시하지 않습니다.

- 실제 사용자 인증과 프로젝트 권한 검증 성공
- 공개 범위 코드 통일과 고객 공개 데이터 검수 완료
- 화면의 수치가 원장 행으로 역추적 가능
- 웹 입력이 원장 행과 활동로그에 함께 기록됨
- 중복 요청과 동시 수정 테스트 통과
- 고객사 간 데이터 격리 테스트 통과
- 백업 생성과 복원 테스트 통과

### 2026-09-03 이슈사항·추가요청 원장

- 기준 `캠페인 스케줄 관리.html` 하단의 `이슈사항 · 추가요청 기록`을 일정표·간트 아래에 동일한 9개 업무 열로 구현했습니다. 샘플 행은 옮기지 않았고 프로젝트마다 빈 원장으로 시작합니다.
- 런타임 원천은 Google Sheets가 아니라 Supabase `project_issues`입니다. 등록일·구분·관련 업무·내용·담당자·상태·완료링크·비고와 생성/수정 사용자, 생성/수정 시각, `row_version`, 보관 시각을 저장합니다.
- 업무와 이슈는 `read_task_workspace` 한 RPC에서 함께 받아 탭 로딩 요청을 늘리지 않습니다. 생성·수정·보관은 `mutate_project_issue` 한 트랜잭션에서 권한, 허용 필드, URL, 동시 수정, 중복 요청을 검사하고 `activity_events`에 감사 이력을 남깁니다.
- 행은 기본 `CLIENT` 공개 범위입니다. 업무 페이지를 볼 수 있는 고객 계정은 읽기만 가능하고 포켓·NS 편집 계정만 쓸 수 있습니다. 원시 테이블의 브라우저 권한은 0건이며 RLS 3개 정책과 제한된 RPC만 노출합니다.
- 원격 DB에서 생성 → 조회 → 수정 → 보관을 한 트랜잭션으로 실행한 뒤 롤백하는 검증을 통과했습니다. 로컬 보안 QA는 23개 공개 테이블 전부 RLS, 미색인 FK 0건, 이슈 URL 거부·낙관적 동시성·보관 제외를 확인합니다.
